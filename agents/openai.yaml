interface:
  display_name: "Guided Learning"
  short_description: "Adaptive tutoring with durable session memory"
  default_prompt: "Use $guided-learning to guide the learner one knowledge node at a time, adapt to observable evidence, and maintain the session's durable learning archive."
